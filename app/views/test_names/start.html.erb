<b><%= label_tag :testname, @test.title %></b>
<%= form_tag(start_test_name_path, :method => 'get')   do |f| %>
  
  
  <%= render "#{@test.current_step}_step", :f => f %>
  
  <%= submit_tag "Back", :name => "back_button" unless @test.first_step? %>
  <%= submit_tag "Next" %>
<% end %>
