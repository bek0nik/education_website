<%= form_for [:admin, @university], :html => {:multipart => true} do |f| %>
  <%= fields_for Image.new do |f| %>
    <%= file_field :name, :rel => admin_university_images_path %>
  <% end %>
  <%= render :partial => 'images/photo', :collection => @university.images %>
<% end -%>
